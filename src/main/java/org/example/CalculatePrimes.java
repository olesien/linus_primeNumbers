package org.example;

import java.util.ArrayList;

public class CalculatePrimes {

    void printCount(ArrayList<Number> nums) {

    }

    void printSum(ArrayList<Number> nums) {

    }
    ArrayList<Number> calculate (int from, int to) {
        ArrayList<Number> primes = new ArrayList<Number>();
        if (from < 0 || to > 1000) {
            throw new IllegalArgumentException("Hoppsan, fel intervall angivet!");
        }

        return primes;
    }
}
